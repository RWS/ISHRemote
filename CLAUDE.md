@AGENTS.md

## Claude Code

Path-scoped rules in `.claude/rules/` mirror the `applyTo:` globs from `.github/instructions/`.
Both sets must stay in sync — see `AGENTS.md` > Maintaining these instructions.
