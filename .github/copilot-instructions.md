# ISHRemote — AI Coding Agent Instructions

The canonical instructions live in [`AGENTS.md`](../AGENTS.md) at the repository root.
Read it first. It contains build, test, lint, and runtime architecture guidance, plus the lazy-load directive pointing to all scoped instruction files in `.github/instructions/`.

The scoped files below are auto-injected via their `applyTo:` frontmatter when you edit a matching file — they are the single source of content for per-domain conventions.
